# Abyssal NGO 2.13.2 candidate

The package at /com.unity.netcode.gameobjects is the exact 1314-file integrated migration candidate, based on Unity NGO 2.13.2 with the reviewed custom external-scene, cache and recovery behavior. Its package version and complete source/metadata bytes match the executed candidate; this release preparation does not reconstruct pristine upstream or add a second compatibility layer.

The existing 1.15.1 patched branch remains the ancestry for this owned fork. Repository tooling and other projects outside the package are retained as historical upstream content, not silently claimed to validate the new package. The Unity 6.6 consumer selects the package subpath explicitly.

Acceptance tags remain deferred until immutable clean-consumer verification; the proposed tag is abyssal-2.13.2-v1. Scoped migration evidence is maintained by the coordinator.
